name = "mizuki"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[env.production]
vars = { ENVIRONMENT = "production" }

# KV命名空间配置（可选）- 用于Cloudflare Pages Functions
[[kv_namespaces]]
binding = "PAN123_CACHE"
id = "JiuYue"
preview_id = "JiuYue"

# 注意：Cloudflare Pages会自动检测pages配置
# 不需要显式设置pages_build_output_dir，Cloudflare Pages会根据框架自动检测